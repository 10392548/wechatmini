<!--index.wxml-->
<navigation-bar title="宠物项圈" back="{{false}}" color="black" background="#FFF"></navigation-bar>
<scroll-view class="page-content" scroll-y show-scrollbar="{{false}}">
  <!-- 宠物信息卡片 -->
  <view class="pet-card">
    <view class="pet-avatar-wrapper">
      <image class="pet-avatar" src="{{pet.avatar || '/images/pet.png'}}" mode="aspectFill"></image>
      <view class="status-dot"></view>
    </view>
    <view class="pet-info">
      <view class="pet-name">{{pet.name}}</view>
      <view class="pet-status">{{pet.status}}</view>
    </view>
    <view class="battery-info">
      <text class="battery-icon">🔋</text>
      <text class="battery-text">{{pet.battery}}%</text>
    </view>
  </view>

  <!-- 今日运动 -->
  <view class="section-card" bindtap="goToActivity">
    <view class="section-header">
      <text class="section-title">今日运动</text>
      <text class="section-more">查看详情 ></text>
    </view>
    <view class="activity-stats">
      <view class="activity-item orange">
        <image class="activity-icon-img" src="/images/Running.png" mode="aspectFit"></image>
        <text class="activity-label">奔跑</text>
        <text class="activity-value">{{todayStats.runningTime}}</text>
        <text class="activity-unit">分钟</text>
      </view>
      <view class="activity-item blue">
        <image class="activity-icon-img" src="/images/Walking.png" mode="aspectFit"></image>
        <text class="activity-label">行走</text>
        <text class="activity-value">{{todayStats.walkingTime}}</text>
        <text class="activity-unit">分钟</text>
      </view>
      <view class="activity-item green">
        <image class="activity-icon-img" src="/images/Still.png" mode="aspectFit"></image>
        <text class="activity-label">静止</text>
        <text class="activity-value">{{todayStats.staticTime}}</text>
        <text class="activity-unit">分钟</text>
      </view>
    </view>
  </view>

  <!-- 设备状态 -->
  <view class="section-card" bindtap="goToBattery">
    <view class="section-header">
      <text class="section-title">设备状态</text>
      <text class="section-more">查看详情 ></text>
    </view>
    <view class="device-status">
      <view class="device-status-main">
        <view class="device-online {{isOnline ? 'online' : 'offline'}}">
          <view class="online-dot"></view>
        </view>
        <view class="device-info">
          <text class="device-status-text">{{isOnline ? '在线' : '离线'}}</text>
          <text class="device-last-online">最后在线: {{lastOnlineTime}}</text>
        </view>
      </view>
      <view class="device-current">
        <view class="current-item">
          <image class="current-icon-img" src="/images/{{currentStatus.icon}}.png" mode="aspectFit"></image>
          <text class="current-text">{{currentStatus.text}}</text>
        </view>
        <view class="current-item temperature">
          <text class="temp-label">温度</text>
          <text class="temp-value">{{temperature}}°C</text>
        </view>
      </view>
    </view>
  </view>

  <!-- 快捷功能 -->
  <view class="section-card">
    <view class="section-header">
      <text class="section-title">快捷功能</text>
    </view>
    <view class="features-grid">
      <view class="feature-item" wx:for="{{features}}" wx:key="id" bindtap="onFeatureTap" data-url="{{item.url}}">
        <view class="feature-icon" style="background: {{item.color}}">
          <image class="feature-icon-img" src="{{item.icon}}" mode="aspectFit"></image>
        </view>
        <text class="feature-name">{{item.name}}</text>
      </view>
    </view>
  </view>

  <!-- 最新动态 -->
  <view class="section-card" bindtap="goToMoments">
    <view class="section-header">
      <text class="section-title">最新动态</text>
      <text class="section-more">查看更多 ></text>
    </view>
    <view class="moment-preview">
      <view class="moment-header">
        <image class="moment-avatar" src="{{latestMoment.avatar || '/images/pet.png'}}" mode="aspectFill"></image>
        <view class="moment-info">
          <text class="moment-nickname">{{latestMoment.nickname}}</text>
          <text class="moment-time">{{latestMoment.time}}</text>
        </view>
      </view>
      <text class="moment-content">{{latestMoment.content}}</text>
      <view class="moment-actions">
        <view class="moment-action">
          <image class="action-icon" src="/images/Like.png" mode="aspectFit"></image>
          <text class="action-count">{{latestMoment.likes}}</text>
        </view>
        <view class="moment-action">
          <image class="action-icon" src="/images/Comment.png" mode="aspectFit"></image>
          <text class="action-count">{{latestMoment.comments}}</text>
        </view>
      </view>
    </view>
  </view>

  <!-- 今日记录 -->
  <view class="section-card" bindtap="goToDiary">
    <view class="section-header">
      <text class="section-title">今日记录</text>
      <text class="section-more">查看全部 ></text>
    </view>
    <view class="diary-list">
      <view class="diary-item" wx:for="{{todayDiary}}" wx:key="id">
        <view class="diary-icon" style="background: {{item.bgColor}}">
          <image class="diary-icon-img" src="{{item.image}}" mode="aspectFit"></image>
        </view>
        <view class="diary-content">
          <text class="diary-title">{{item.title}}</text>
          <text class="diary-desc">{{item.description}}</text>
        </view>
        <text class="diary-time">{{item.time}}</text>
      </view>
    </view>
  </view>

  <!-- 底部间距 -->
  <view style="height: 40rpx"></view>
</scroll-view>
