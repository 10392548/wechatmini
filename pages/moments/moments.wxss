/**moments.wxss**/
page {
  height: 100vh;
  display: flex;
  flex-direction: column;
  background: linear-gradient(180deg, #FFF9F5 0%, #FFF0E8 100%);
}

/* 悬浮发布按钮 */
.fab {
  position: fixed;
  right: 32rpx;
  bottom: 100rpx;
  width: 112rpx;
  height: 112rpx;
  background: linear-gradient(135deg, #FFB88C 0%, #FF8C69 100%);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 8rpx 24rpx rgba(255, 140, 105, 0.4);
  z-index: 100;
  transition: all 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
}

.fab:active {
  transform: scale(0.9);
}

.fab-icon {
  font-size: 64rpx;
  color: #FFFFFF;
  font-weight: 300;
  line-height: 1;
}

.page-content {
  flex: 1;
  display: flex;
  flex-direction: column;
  overflow-y: auto;
}

/* 内容区域 */
.content {
  flex: 1;
  padding: 20rpx;
}
.moment-item {
  background: #FFFFFF;
  border-radius: 24rpx;
  padding: 32rpx;
  margin-bottom: 24rpx;
  box-shadow: 0 4rpx 16rpx rgba(255, 140, 105, 0.08);
}
.moment-header {
  display: flex;
  align-items: center;
  margin-bottom: 20rpx;
}
.avatar {
  width: 80rpx;
  height: 80rpx;
  border-radius: 50%;
  margin-right: 20rpx;
  border: 3rpx solid #FFFFFF;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.08);
}
.user-info {
  flex: 1;
}
.nickname {
  font-size: 32rpx;
  font-weight: bold;
  color: #333;
  margin-bottom: 8rpx;
}
.time-location {
  font-size: 24rpx;
  color: #B0B0B0;
}

.moment-content {
  font-size: 30rpx;
  color: #2D2D2D;
  line-height: 1.6;
  margin-bottom: 20rpx;
}

/* 图片网格 */
.images-grid {
  display: flex;
  flex-wrap: wrap;
  gap: 8rpx;
  margin-bottom: 20rpx;
}

.moment-image {
  border-radius: 12rpx;
}

/* 1张图片 */
.images-1 .moment-image {
  width: 100%;
  height: 400rpx;
}

/* 2-4张图片 */
.images-2 .moment-image,
.images-3 .moment-image,
.images-4 .moment-image {
  width: calc(50% - 4rpx);
  height: 220rpx;
}

/* 5-9张图片 */
.images-5 .moment-image,
.images-6 .moment-image,
.images-7 .moment-image,
.images-8 .moment-image,
.images-9 .moment-image {
  width: calc(33.33% - 6rpx);
  height: 220rpx;
}
.moment-footer {
  display: flex;
  align-items: center;
}
.action-item {
  display: flex;
  align-items: center;
  margin-right: 60rpx;
}
.action-icon {
  width: 36rpx;
  height: 36rpx;
  margin-right: 12rpx;
}
.icon {
  font-size: 32rpx;
}
.count {
  font-size: 24rpx;
  color: #999;
}
.action-share {
  margin-left: auto;
}

/* 点赞状态 */
.action-item.liked .count {
  color: #FF8C69;
}

/* 评论区域 */
.comments-section {
  margin-top: 20rpx;
  padding-top: 20rpx;
  border-top: 1rpx solid #f0f0f0;
}
.comment-item {
  margin-bottom: 16rpx;
}
.comment-main {
  margin-bottom: 8rpx;
}
.comment-nickname {
  font-size: 26rpx;
  color: #576b95;
  margin-right: 12rpx;
}
.comment-content {
  font-size: 26rpx;
  color: #333;
}
.comment-time {
  font-size: 22rpx;
  color: #999;
  margin-left: 12rpx;
}
.reply-item {
  margin-top: 8rpx;
  padding-left: 24rpx;
}
.reply-nickname {
  font-size: 24rpx;
  color: #576b95;
  margin-right: 12rpx;
}
.reply-content {
  font-size: 24rpx;
  color: #333;
}
.reply-time {
  font-size: 20rpx;
  color: #999;
  margin-left: 12rpx;
}
