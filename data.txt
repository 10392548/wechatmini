 1. 删除范围：
    - 只删除界面上的年龄输入框？
    - 还是也要删除相关的 JS 代码（onAgeInput 方法）？
    - 保存时是否还要传递年龄数据到后端？
删除界面上的年龄输入框
  2. 数据处理：
    - 如果宠物已有年龄数据，删除字段后如何处理？保留原值还是清空？
删除页面的输入框即可
  关于头像上传修复

  3. 保存时机：
    - 用户选择头像后立即保存到服务器（无需点击"保存"按钮）？
    - 还是只在点击"保存"按钮时一起保存？
立即保存，然后读取的时候还需要读这个，也可以点击保存的时候保存，那个方便按照那个来
  4. 用户反馈：
    - 如果立即保存，上传成功后是否需要显示"头像已更新"的提示？
    - 上传失败时如何处理（当前只有 toast 提示）？

  关于 my 页面头像显示

  5. 默认头像：
    - 如果用户没有上传头像（avatar_url 为空），是否显示默认的 /images/pet.png？
是的
  6. 全局状态更新：
    - 是否需要在保存资料后更新 app.globalData.userInfo，以保持全局状态同步？
是的